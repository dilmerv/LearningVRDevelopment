using UnityEngine;

public class TestingMetaInspector : MonoBehaviour
{
    private float mugFallSpeed = 29.0f;

    private Vector3 mugPositionRelativeToTheGuitar = new Vector3(1, 2, 3);

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
